import React from 'react';
import "./modal.scss"
const Modal = ({cklicked}) => {
    return (
        <div className='modal' onClick={cklicked}>
            
        </div>
    );
};

export default Modal;